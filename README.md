For using this application you need to download the
repository and run the command line npm install to
install all dependency.
Then you can use npm run start for running Client 
and server in same time
